project: test-fixture
cdd_spec: 1.1.0
version: 1.0.0
status: draft

goal: |
  A simple test fixture to validate CDD tooling.

success_criteria:
  - Lint passes
  - Tests run
  - Report is valid

components:
  - calculator
